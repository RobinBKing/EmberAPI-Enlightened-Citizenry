import { chamberSelect } from 'enlightened-citizenry/helpers/chamber-select';
import { module, test } from 'qunit';

module('Unit | Helper | chamber select');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = chamberSelect([42]);
  assert.ok(result);
});
